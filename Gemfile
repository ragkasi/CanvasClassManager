# frozen_string_literal: true

source "https://rubygems.org"

# To add a dependency that is installed with "gem install package", just add:
# gem 'package'
gem 'dotenv', groups: [:development, :test] # To load .env variables
gem 'httparty' # http requests
gem 'nokogiri'
gem 'open-uri'


